/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  pageExtensions: ["mdx", "md", "jsx", "js", "ts", "tsx", "bs.js"],
  reactStrictMode: true,
};

module.exports = nextConfig;
